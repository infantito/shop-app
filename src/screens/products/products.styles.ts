import { StyleSheet } from 'react-native'

const productsOverviewStyles = StyleSheet.create({
  centered: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
  },
})

const userProductsStyles = StyleSheet.create({
  empty: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
  },
})

export { productsOverviewStyles, userProductsStyles }
